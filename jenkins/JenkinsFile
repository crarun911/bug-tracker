pipeline {
    agent any

    stages {
        stage('Unit Tests - Backened') {
            steps {
                dir('bugtracker-backend'){
                    go test -v ./...
                }
        }
    }
}