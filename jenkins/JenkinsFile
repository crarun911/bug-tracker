pipeline {
    agent any

    stages {
        stage('Unit Tests - Backened') {
            steps {
                dir('bugtracker-backend'){
                    sh 'go test -v ./...'
                }
        }
    }
}